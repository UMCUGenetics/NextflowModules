*Creating (and running) singularity images using GNU guix*

1) guixr pack -f squashfs -S /bin=bin bash fastqc
2) Copy *squashfs to appropriate directory (maybe rename)
3) Add to nextflow.config:
singularity {
  autoMounts = true
}
4) Run nextflow:
nextflow run whatever.nf -with-singularity singularity-image.squashfs
